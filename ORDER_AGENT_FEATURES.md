# 订单 Agent 功能说明

## 概述

订单 Agent (`OrderAgent`) 是云边奶茶铺系统中负责处理所有订单相关业务的智能体，通过 MCP (Model Context Protocol) 工具与订单服务交互。

## 支持的功能

### 1. 📦 创建订单 (`order-create-order`)

**功能描述**：为用户创建新的奶茶订单，支持单个或多个产品。

**工具名称**：`order-create-order`

**参数**：
- `userId` (integer, 必需): 用户ID，必须为正整数
- `items` (array, 必需): 订单项列表，每个订单项包含：
  - `productName` (string, 必需): 产品名称，必须是云边奶茶铺的现有产品
  - `sweetness` (string, 必需): 甜度要求，可选值：`无糖`、`微糖`、`半糖`、`少糖`、`标准糖`
  - `iceLevel` (string, 必需): 冰量要求，可选值：`热`、`温`、`去冰`、`少冰`、`正常冰`
  - `quantity` (integer, 必需): 购买数量，必须为正整数，默认为1
  - `remark` (string, 可选): 订单项备注
- `remark` (string, 可选): 订单整体备注

**功能特点**：
- ✅ 支持单个产品订单
- ✅ 支持多个产品订单（一次下单多个产品）
- ✅ 自动检查库存
- ✅ 自动计算总价格
- ✅ 支持订单备注和订单项备注

**使用示例**：
```
用户："我要一杯云边茉莉，少糖，正常冰"
→ 创建单个产品订单

用户："我要一杯云边茉莉和两杯桂花云露"
→ 创建多产品订单
```

---

### 2. 🔍 查询订单（根据订单ID）(`order-get-order`)

**功能描述**：根据订单ID查询订单的详细信息。

**工具名称**：`order-get-order`

**参数**：
- `orderId` (string, 必需): 订单ID，格式为 `ORDER_` 开头的唯一标识符，例如：`ORDER_1693654321000`

**返回信息**：
- 订单ID
- 用户ID
- 订单总价
- 订单状态
- 创建时间
- 订单项详情（产品名称、甜度、冰量、数量、单价、小计）
- 订单备注

**使用示例**：
```
用户："查询订单 ORDER_1693654321000"
→ 返回该订单的完整信息
```

---

### 3. 🔍 查询订单（根据用户ID和订单ID）(`order-get-order-by-user`)

**功能描述**：根据用户ID和订单ID查询订单的详细信息，确保只能查询属于该用户的订单。

**工具名称**：`order-get-order-by-user`

**参数**：
- `userId` (integer, 必需): 用户ID，必须为正整数
- `orderId` (string, 必需): 订单ID，格式为 `ORDER_` 开头的唯一标识符

**安全特性**：
- ✅ 只能查询属于指定用户的订单
- ✅ 防止跨用户查询订单

**使用示例**：
```
用户："查询我的订单 ORDER_1693654321000"
→ 使用当前会话的 userId 查询订单
```

---

### 4. 📋 查询订单列表 (`order-get-orders-by-user`)

**功能描述**：根据用户ID获取该用户的所有订单列表。

**工具名称**：`order-get-orders-by-user`

**参数**：
- `userId` (integer, 必需): 用户ID，必须为正整数

**返回信息**：
- 订单列表（按创建时间排序）
- 每个订单包含：订单ID、产品信息、价格、创建时间

**使用示例**：
```
用户："我想查询我的历史订单"
用户："查看我的所有订单"
→ 返回该用户的所有订单列表
```

---

### 5. 🗑️ 删除订单 (`order-delete-order`)

**功能描述**：根据用户ID和订单ID删除订单。

**工具名称**：`order-delete-order`

**参数**：
- `userId` (integer, 必需): 用户ID，必须为正整数
- `orderId` (string, 必需): 订单ID，格式为 `ORDER_` 开头的唯一标识符

**安全特性**：
- ✅ 只能删除属于指定用户的订单
- ✅ 防止跨用户删除订单
- ✅ 需要确认用户ID和订单号匹配

**使用示例**：
```
用户："取消订单 ORDER_1693654321000"
用户："删除我的订单 ORDER_1693654321000"
→ 删除指定订单
```

---

### 6. ✏️ 更新订单备注 (`order-update-remark`)

**功能描述**：根据用户ID和订单ID更新订单备注。

**工具名称**：`order-update-remark`

**参数**：
- `userId` (integer, 必需): 用户ID，必须为正整数
- `orderId` (string, 必需): 订单ID，格式为 `ORDER_` 开头的唯一标识符
- `remark` (string, 必需): 新的备注内容

**安全特性**：
- ✅ 只能更新属于指定用户的订单
- ✅ 防止跨用户更新订单

**使用示例**：
```
用户："修改订单 ORDER_1693654321000 的备注为：不要珍珠"
→ 更新订单备注
```

---

## 功能分类

### 订单创建类
- ✅ 创建订单（单产品/多产品）

### 订单查询类
- ✅ 根据订单ID查询
- ✅ 根据用户ID和订单ID查询
- ✅ 查询用户的所有订单列表

### 订单管理类
- ✅ 删除订单
- ✅ 更新订单备注

---

## 安全约束

根据 `OrderAgent` 的系统提示词，有以下安全约束：

1. **下单约束**：
   - 必须提供 userId
   - 只有用户ID存在时才允许下单

2. **查询约束**：
   - 必须提供 userId 或订单号
   - 只能根据具体的用户ID或订单号查询相应的订单
   - **绝对不允许查询或操作其他用户订单**

3. **修改/删除约束**：
   - 必须和用户确认用户ID和订单号
   - 确保用户ID和订单号匹配且唯一后才允许修改或删除订单
   - 一次只允许修改一个订单

---

## 支持的产品

根据代码，系统支持以下产品：
- 云边茉莉
- 桂花云露
- 云雾观音
- 珍珠奶茶
- 红豆奶茶

---

## 支持的甜度选项

- 无糖
- 微糖
- 半糖
- 少糖
- 标准糖

---

## 支持的冰量选项

- 热
- 温
- 去冰
- 少冰
- 正常冰

---

## 工作流程

1. **接收用户请求**：OrderAgent 接收用户的订单相关请求
2. **分析请求类型**：使用 LLM 判断需要使用哪个工具
3. **提取参数**：从用户输入中提取工具所需的参数
4. **调用工具**：通过 MCP 协议调用 order-mcp-server 的工具
5. **整合结果**：将工具返回的结果整合成用户友好的回复

---

## 技术实现

- **协议**：MCP (Model Context Protocol)
- **服务**：order-mcp-server (运行在端口 10002)
- **数据库**：支持 SQLite 和 MySQL
- **LLM**：使用 DashScope (通义千问) 进行意图识别和参数提取

---

## 使用示例

### 示例 1: 创建单产品订单
```
用户："我要一杯云边茉莉，少糖，正常冰"

OrderAgent 处理：
1. 识别为创建订单请求
2. 提取参数：
   - userId: 当前会话用户ID
   - items: [{
       productName: "云边茉莉",
       sweetness: "少糖",
       iceLevel: "正常冰",
       quantity: 1
     }]
3. 调用 order-create-order 工具
4. 返回订单创建成功信息
```

### 示例 2: 创建多产品订单
```
用户："我要一杯云边茉莉，少糖，正常冰，还有两杯桂花云露，半糖，去冰"

OrderAgent 处理：
1. 识别为创建订单请求
2. 提取参数：
   - userId: 当前会话用户ID
   - items: [
       {
         productName: "云边茉莉",
         sweetness: "少糖",
         iceLevel: "正常冰",
         quantity: 1
       },
       {
         productName: "桂花云露",
         sweetness: "半糖",
         iceLevel: "去冰",
         quantity: 2
       }
     ]
3. 调用 order-create-order 工具
4. 返回订单创建成功信息（包含总价）
```

### 示例 3: 查询历史订单
```
用户："我想查询我的历史订单"

OrderAgent 处理：
1. 识别为查询订单列表请求
2. 提取参数：
   - userId: 当前会话用户ID
3. 调用 order-get-orders-by-user 工具
4. 返回该用户的所有订单列表
```

---

## 总结

订单 Agent 提供了完整的订单生命周期管理功能：
- ✅ 创建订单（支持单产品/多产品）
- ✅ 查询订单（单个/列表）
- ✅ 删除订单
- ✅ 更新订单备注

所有功能都通过 MCP 工具实现，确保了模块化和可扩展性。

